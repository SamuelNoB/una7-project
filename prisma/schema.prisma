// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model User {
  id            Int             @id @default(autoincrement())
  createdAt     DateTime        @default(now())
  userName      String
  password      String
  email         String          @unique

  publications  Publication[]   
}

model Publication {
  id            Int         @id @default(autoincrement())
  active        Boolean     @default(true)
  title         String
  subTitle      String
  coverImageId  Int         // imagem de capa da publicação
  content       String

  createdAt     DateTime    @default(now())
  updatedAt     DateTime    @default(now())

  authorId      Int
  author        User        @relation(fields: [authorId], references: [id])

}

model Partner {
  id            Int         @id @default(autoincrement())
  name          String
  pageLink      String
  active        Boolean     @default(true)
  imageId       Int         @unique // se relaciona com o model File
}

model Client {
  id            Int         @id @default(autoincrement())
  visible       Boolean     @default(true)
  name          String
  clientPhotoId Int
  link          String?
}

model Contact {
  id            Int         @id @default(autoincrement())
  subject       String
  senderName    String
  email         String
  content       String
}


model File {
  id        Int     @id @default(autoincrement())
  name      String
  content   Bytes
}

